# DNF游戏自动刷图程序 - 项目技术文档

> 版本：v2.0
> 更新日期：2026-04-10
> 项目类型：Python + YOLOv8 PC游戏自动化辅助程序

---

## 一、项目概述

### 1.1 核心定位

本项目是一个基于**计算机视觉（YOLO目标检测）**和**自动化输入控制**的DNF（地下城与勇士）游戏辅助脚本。通过实时屏幕捕获、目标识别和智能决策逻辑，实现游戏角色的自动刷图、释放技能、翻牌、捡装备、换人等全流程自动化。

### 1.2 技术栈

| 技术分类 | 具体技术 | 用途说明 |
|---------|---------|---------|
| **目标检测** | Ultralytics YOLOv8 | 识别游戏画面中的角色、怪物、门、道具等 |
| **图像处理** | OpenCV, NumPy | 图像预处理、颜色分析、几何计算 |
| **屏幕捕获** | MSS, Win32 GDI | 高性能屏幕/窗口区域捕获 |
| **输入控制** | pynput | 模拟键盘鼠标操作，含人性化延迟 |
| **几何计算** | Shapely | 路径规划与碰撞检测 |
| **OCR识别** | PaddleOCR | 商店界面文字识别 |
| **定时调度** | APScheduler | 定时任务配置 |
| **配置管理** | PyYAML | YAML配置文件解析 |

### 1.3 支持的游戏模式

| 模式 | 配置值 | 说明 |
|-----|-------|-----|
| 深渊模式 | `abyss` | 无限刷深渊副本 |
| 白图模式 | `white_map` | 普通副本刷图，支持门导航 |
| 新深渊模式 | `new_abyss` | 创新世纪版本的深渊系统 |

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              主控制层 (main.py)                              │
│                          命令行解析 / 配置加载 / 入口调度                      │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           游戏引擎层 (engine.py)                             │
│                    GameEngine - 协调所有模块的主控制器                         │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┬─────────────┐   │
│  │ 屏幕捕获    │ 目标检测    │ 输入控制     │ 状态机       │ 上下文管理   │   │
│  └─────────────┴─────────────┴─────────────┴─────────────┴─────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
          ┌───────────────────────────┼───────────────────────────┐
          ▼                           ▼                           ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   捕获模块       │     │   检测模块       │     │   控制模块       │
│   capture/      │     │   detection/     │     │   control/      │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ • MSSCapture    │     │ • YOLODetector   │     │ • InputController│
│ • WindowManager │     │ • MultiModelDetector│  │ • 键鼠模拟      │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              决策与业务逻辑层                                  │
│  ┌──────────────┬──────────────┬──────────────┬──────────────┬────────────┐ │
│  │ GameContext  │ StateMachine │ SkillManager │ BuffManager  │ MapNavigator│ │
│  └──────────────┴──────────────┴──────────────┴──────────────┴────────────┘ │
│  ┌──────────────┬──────────────┬──────────────┬──────────────┬────────────┐ │
│  │ CardFlipper  │ StuckHandler │ CharacterSwitch│MultiCharacter│ Selector  │ │
│  └──────────────┴──────────────┴──────────────┴──────────────┴────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 模块依赖关系

```
main.py
    │
    ▼
GameEngine
    │
    ├── MSSCapture (屏幕捕获)
    │       └── MSS库 / Win32 API
    │
    ├── YOLODetector / MultiModelDetector (目标检测)
    │       └── Ultralytics YOLOv8
    │
    ├── InputController (输入控制)
    │       └── pynput
    │
    ├── GameContext (游戏上下文)
    │
    ├── StateMachine (状态机)
    │
    └── 业务模块
            ├── SkillManager (技能管理)
            ├── BuffManager (Buff管理)
            ├── MapNavigator (地图导航)
            ├── CardFlipper (翻牌处理)
            ├── StuckHandler (卡住恢复)
            ├── CharacterSwitcher (角色切换)
            ├── MultiCharacterManager (多角色管理)
            └── SelectionManager (选择管理)
```

---

## 三、核心模块详解

### 3.1 屏幕捕获模块 (`src/capture/`)

#### 3.1.1 MSSCapture - 高性能屏幕捕获

**文件位置**: `src/capture/mss_capture.py`

**核心功能**:
- 基于MSS库的高性能屏幕捕获
- 支持指定区域捕获
- 内置FPS统计和性能监控

**关键API**:

```python
class MSSCapture:
    def capture(self) -> np.ndarray:
        """捕获整个显示器，返回BGR格式图像"""
        
    def capture_region(self, region: Tuple[int, int, int, int]) -> np.ndarray:
        """
        捕获指定区域
        Args: region = (left, top, width, height)
        Returns: BGR格式numpy数组
        """
        
    def capture_window(self, left, top, width, height) -> np.ndarray:
        """捕获窗口区域"""
```

#### 3.1.2 WindowManager - 窗口管理

**文件位置**: `src/capture/window_manager.py`

**核心功能**:
- 查找游戏窗口
- 获取窗口客户区坐标
- 窗口置顶操作
- 列出系统所有可见窗口

**关键API**:

```python
class WindowManager:
    def find_window(self) -> bool:
        """查找指定标题的窗口"""
        
    def bring_to_front(self) -> None:
        """将窗口置顶"""
        
    def get_client_screen_rect(self) -> Tuple[int, int, int, int]:
        """获取窗口客户区在屏幕上的坐标 (left, top, right, bottom)"""
        
    @staticmethod
    def list_all_windows() -> List[Tuple[int, str]]:
        """列出所有可见窗口，返回 [(hwnd, title), ...]"""
```

---

### 3.2 目标检测模块 (`src/detection/`)

#### 3.2.1 YOLODetector - YOLOv8检测器

**文件位置**: `src/detection/detector.py`

**核心功能**:
- YOLOv8模型加载与推理
- 单帧/批量检测
- 模型预热加速

**Detection数据结构**:

```python
@dataclass
class Detection:
    class_id: int           # 类别ID
    class_name: str         # 类别名称
    confidence: float       # 置信度
    bbox: Tuple[int,int,int,int]  # (x1, y1, x2, y2)
    center: Tuple[int,int]  # 中心点坐标
    width: int             # 宽度
    height: int            # 高度
    area: int              # 面积
```

**支持的检测类别**:

| 类别名称 | 说明 | 映射到 |
|---------|-----|-------|
| `monster` | 普通怪物 | enemies |
| `hero` | 英雄/精英怪 | enemies |
| `elite` | 精英怪 | enemies |
| `boss` | Boss怪物 | enemies |
| `boss-m` | 小地图Boss标识 | enemies |
| `door` | 门/传送门 | doors |
| `article` | 拾取物品 | items |
| `purple_card` | 紫色卡片 | items/cards |
| `brand` | 翻牌界面 | ui_elements |
| `menu` | 结算菜单 | ui_elements |
| `shop` | 商店界面 | ui_elements |
| `people` | 角色位置 | players |

#### 3.2.2 MultiModelDetector - 多模型协同检测

**核心功能**:
- 支持同时加载多个YOLO模型
- 按名称分组检测结果
- 统一的结果聚合接口

---

### 3.3 输入控制模块 (`src/control/`)

#### 3.3.1 InputController - 统一输入控制器

**文件位置**: `src/control/input_controller.py`

**核心功能**:
- 键盘按键模拟（支持组合键）
- 鼠标移动、点击、拖拽
- **贝塞尔曲线平滑移动**（仿人类轨迹）
- **人性化随机延迟**（防检测）

**人性化设计**:

```python
class InputController:
    def _smooth_move(self, target_x: int, target_y: int) -> None:
        """贝塞尔曲线平滑移动，模拟人类鼠标轨迹"""
        # 生成随机控制点
        # 根据距离计算移动时间
        # 60fps插值移动
        
    def _get_random_delay(self) -> float:
        """生成随机延迟，范围 [0.02, 0.08] 秒"""
```

**横版游戏控制**:

```python
def move_left(self, duration: float) -> None:
    """向左移动"""

def move_right(self, duration: float) -> None:
    """向右移动"""

def start_moving(self, direction: str) -> None:
    """开始持续移动"""

def stop_moving(self, direction: str = None) -> None:
    """停止移动"""

def attack_continuous(self, key: str = 'x', count: int = 3) -> None:
    """连续攻击"""
```

---

### 3.4 决策模块 (`src/decision/`)

#### 3.4.1 GameContext - 游戏上下文

**文件位置**: `src/decision/game_context.py`

**核心功能**:
- 当前帧检测结果存储
- 游戏状态管理
- 历史帧记录（deque，保留30帧）
- DNF专用字段管理

**游戏状态枚举 (GameState)**:

```python
class GameState(Enum):
    MENU = "menu"                    # 菜单/结算界面
    LOADING = "loading"              # 加载中
    PLAYING = "playing"              # 探索中
    COMBAT = "combat"                # 战斗中
    TRANSITIONING = "transitioning"  # 地图过渡
    PAUSED = "paused"               # 暂停
    DEAD = "dead"                   # 死亡
    VICTORY = "victory"             # 胜利
    CARD_FLIPPING = "card_flipping" # 翻牌
    CHARACTER_SWITCH = "character_switch"  # 角色切换
    STUCK_RECOVERY = "stuck_recovery"      # 卡住恢复
    BUFFING = "buffing"                    # 释放Buff
    MENU_SELECTING = "menu_selecting"      # 菜单选择
```

**DNF专用字段**:

```python
# 地图追踪
self.map_index: int = 0           # 当前房间索引
self.door_stuck_count: int = 0    # 卡门计数
self.player_stuck_count: int = 0  # 玩家卡住计数

# 刷图统计
self.is_abyss_mode: bool = False   # 是否深渊模式
self.dungeon_run_count: int = 0    # 当前刷图次数
self.max_dungeon_runs: int = 16    # 最大刷图次数

# 菜单检测
self.menu_detect_count: int = 0   # menu连续检测次数
self.menu_detect_threshold: int = 180  # 超此值触发角色切换
```

#### 3.4.2 StateMachine - 状态机

**文件位置**: `src/decision/state_machine.py`

**核心功能**:
- 根据检测结果自动更新游戏状态
- 定义状态转换规则
- 支持自定义状态回调

#### 3.4.3 SkillManager - 技能管理器

**文件位置**: `src/decision/skill_manager.py`

**核心功能**:
- 技能队列管理
- 冷却时间追踪
- 优先级自动释放

#### 3.4.4 BuffManager - Buff管理

**文件位置**: `src/decision/buff_manager.py`

**核心功能**:
- 进图自动释放Buff
- Buff冷却管理
- 定时刷新Buff

#### 3.4.5 MapNavigator - 地图导航

**文件位置**: `src/decision/map_navigator.py`

**核心功能**:
- 门识别与导航
- 小地图Hero图标追踪
- 预设路线执行
- Boss房间识别

#### 3.4.6 CardFlipper - 翻牌处理

**文件位置**: `src/decision/card_flipper.py`

**核心功能**:
- 检测翻牌界面
- **紫卡优先选择**策略
- 随机选牌兜底

#### 3.4.7 StuckHandler - 卡住恢复

**文件位置**: `src/decision/stuck_handler.py`

**卡住类型**:

```python
class StuckType(Enum):
    DOOR_STUCK = "door_stuck"      # 卡门：door_count >= 5
    PLAYER_STUCK = "player_stuck"  # 卡角色：people_count > 4
```

**恢复策略**:
- 卡门：向左跑脱困
- 卡角色：调整位置

#### 3.4.8 CharacterSwitcher - 角色切换

**文件位置**: `src/decision/character_switcher.py`

**核心功能**:
- OCR商店检测
- 返回角色选择菜单
- 自动切换下一角色

**切换流程**:

```
1. 检测是否有商店界面 → 按ESC关闭
2. 按F12打开换人界面
3. 按ESC确认
4. 鼠标点击切换角色按钮
5. 按right键切换到下一个角色
6. 重新进入副本
```

#### 3.4.9 MultiCharacterManager - 多角色管理

**文件位置**: `src/decision/multi_character_manager.py`

**核心功能**:
- 多角色轮换调度
- 角色独立刷图次数统计
- 范围角色切换（start_name ~ end_name）

---

### 3.5 选择模块 (`src/selection/`)

#### 3.5.1 SelectionManager - 选择管理器

**文件位置**: `src/selection/selector.py`

**DNF进图流程**:

```
1. 按下空格，等待0.15秒
2. 按下right键5秒
3. 按下空格，等待0.15秒
4. 按下left键2秒
```

---

## 四、主引擎运行流程

### 4.1 初始化流程

```
GameEngine.__init__()
    │
    ├── _init_components()
    │       ├── WindowManager() - 窗口管理
    │       ├── MSSCapture() - 屏幕捕获
    │       ├── YOLODetector/MultiModelDetector - 检测器
    │       ├── InputController() - 输入控制
    │       ├── GameContext() - 游戏上下文
    │       ├── create_game_state_machine() - 状态机
    │       ├── DebugVisualizer() - 调试窗口
    │       │
    │       ├── _init_character_system()
    │       │       └── SkillManager() - 技能管理
    │       │
    │       └── _init_dnf_modules()
    │               ├── BuffManager() - Buff管理
    │               ├── MapNavigator() - 地图导航
    │               ├── CardFlipper() - 翻牌处理
    │               ├── StuckHandler() - 卡住恢复
    │               ├── CharacterSwitcher() - 角色切换
    │               ├── MultiCharacterManager() - 多角色
    │               ├── DungeonRunner() - 副本运行器
    │               └── Scheduler() - 定时调度
    │
    └── 完成初始化
```

### 4.2 主循环流程

```python
def _main_loop(self) -> None:
    """主循环"""
    while self._running:
        if not self._paused:
            self._process_frame()
        # 帧率控制
        # OpenCV窗口事件处理 (P暂停/Q退出)
```

### 4.3 单帧处理流程

```
_process_frame()
    │
    ├── 1. 屏幕捕获
    │       └── capture_region(client_rect) → image
    │
    ├── 2. 目标检测
    │       └── _run_detection(image) → detections
    │
    ├── 3. 更新上下文
    │       └── context.update(detections)
    │
    ├── 4. 更新技能状态
    │       └── skill_manager.update()
    │
    ├── 5. 状态机决策
    │       └── state_machine.update(context)
    │
    ├── 6. 执行状态回调
    │       └── state_callbacks[current_state](context)
    │
    ├── 7. 执行默认动作
    │       └── _execute_action(current_state)
    │               │
    │               ├── 检测menu → 菜单处理
    │               ├── should_switch_character() → 角色切换
    │               │
    │               ├── COMBAT → _combat_action_side_scroller()
    │               │       ├── 获取最近敌人
    │               ├── 计算距离和方向
    │               ├── 在攻击范围内 → 停止移动+攻击
    │               └── 不在范围内 → 移动靠近
    │               │
    │               ├── PLAYING → _playing_action()
    │               │       ├── 无敌人无门 → 向右移动探索
    │               └── 有物品 → 拾取
    │               │
    │               ├── TRANSITIONING → _transitioning_action()
    │               │       └── 移动到门位置
    │               │
    │               ├── CARD_FLIPPING → _card_flipping_action()
    │               │       └── card_flipper.execute_flip()
    │               │
    │               ├── STUCK_RECOVERY → _stuck_recovery_action()
    │               │       └── stuck_handler.execute_recovery()
    │               │
    │               └── BUFFING → _buffing_action()
    │                       └── buff_manager.apply_buffs()
    │
    ├── 8. 调试输出
    │       └── visualizer.draw_all()
    │
    └── 9. 定时保存截图
            └── _save_screenshot_periodically()
```

---

## 五、数据流程图

### 5.1 感知-决策-执行循环

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│    ┌─────────────────────────────────────────────────────┐     │
│    │                   感知层 (Perception)                │     │
│    │                                                     │     │
│    │   ┌───────────┐    ┌───────────┐    ┌───────────┐  │     │
│    │   │ MSSCapture│───▶│  YOLOv8    │───▶│  检测结果  │  │     │
│    │   │  屏幕捕获  │    │   推理     │    │  分类映射  │  │     │
│    │   └───────────┘    └───────────┘    └───────────┘  │     │
│    │                                              │       │     │
│    │                                              ▼       │     │
│    └──────────────────────────────────────────── GameContext │
│                                                                 │
│                              │                                   │
│                              ▼                                   │
│    ┌─────────────────────────────────────────────────────┐     │
│    │                   决策层 (Decision)                  │     │
│    │                                                     │     │
│    │   ┌───────────┐    ┌───────────┐    ┌───────────┐  │     │
│    │   │ StateMachine│──▶│  状态更新  │───▶│ 动作决策  │  │     │
│    │   │  状态机    │    │           │    │           │  │     │
│    │   └───────────┘    └───────────┘    └───────────┘  │     │
│    │                                              │       │     │
│    │   ┌───────────┐    ┌───────────┐           │       │     │
│    │   │ SkillManager│   │ BuffManager│          │       │     │
│    │   │ 技能管理   │    │  Buff管理  │           │       │     │
│    │   └───────────┘    └───────────┘           │       │     │
│    └──────────────────────────────────────────────│───────┘     │
│                                                     │             │
│                                                     ▼             │
│    ┌─────────────────────────────────────────────────────┐     │
│    │                   执行层 (Action)                    │     │
│    │                                                     │     │
│    │   ┌─────────────────────────────────────────────┐  │     │
│    │   │              InputController                 │  │     │
│    │   │                                             │  │     │
│    │   │   • key_press() - 按键                      │  │     │
│    │   │   • key_down/up() - 持续按键                │  │     │
│    │   │   • mouse_move() - 贝塞尔曲线移动           │  │     │
│    │   │   • mouse_click() - 点击                    │  │     │
│    │   │                                             │  │     │
│    │   │   + 人性化随机延迟 (防检测)                   │  │     │
│    │   └─────────────────────────────────────────────┘  │     │
│    │                                                     │     │
│    └─────────────────────────────────────────────────────┘     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 六、配置系统

### 6.1 配置文件结构 (`config/settings.yaml`)

```yaml
# ============ 游戏基础配置 ============
game:
  window_title: "地下城与勇士：创新世纪"  # 游戏窗口标题
  target_fps: 30                            # 目标帧率

# ============ 屏幕捕获配置 ============
capture:
  method: "mss"           # 捕获方法
  monitor_index: 1        # 显示器索引

# ============ 目标检测配置 ============
detection:
  device: "cuda"          # 推理设备: cuda/cpu
  models:
    main:
      path: "models/best_sy.pt"    # 模型路径
      conf_threshold: 0.1           # 置信度阈值
      iou_threshold: 0.1           # IOU阈值

# ============ 输入控制配置 ============
control:
  humanize: true                    # 启用人性化延迟
  random_delay_range: [0.02, 0.08]  # 延迟范围(秒)

# ============ 横版游戏配置 ============
side_scroller:
  attack_key: "x"                   # 攻击按键
  attack_range: 100                # 攻击范围(像素)
  approach_threshold: 50           # 靠近阈值

# ============ DNF副本配置 ============
dungeon:
  mode: "new_abyss"               # abyss/white_map/new_abyss
  max_runs: 16                     # 每角色最大刷图次数
  auto_sell: false                 # 自动卖装备
  collect_items: true              # 自动拾取
  menu_detect_threshold: 180      # 菜单检测阈值

# ============ 翻牌配置 ============
card_flip:
  enabled: true
  purple_priority: true           # 紫卡优先
  random_selection: true          # 随机选牌

# ============ 卡住恢复配置 ============
stuck_recovery:
  door_threshold: 5                # 卡门判定阈值
  player_threshold: 4             # 卡角色判定阈值

# ============ 多角色配置 ============
multi_character:
  enabled: false
  role_list:
    - id: "muse"
      name: "缪斯"
      dungeon_runs: 16
      buffs:
        - keys: ["shift", "1"]
          cooldown: 30
```

---

## 七、使用指南

### 7.1 快速启动

```bash
# 安装依赖
pip install -r requirements.txt

# 使用默认配置运行
python main.py

# 指定窗口和模型
python main.py -w "地下城与勇士" -m models/best.pt

# 使用CPU推理
python main.py -d cpu

# 列出所有窗口
python main.py --list-windows
```

### 7.2 热键控制

| 按键 | 功能 |
|-----|------|
| P | 暂停/恢复主循环 |
| Q / ESC | 退出程序 |

### 7.3 运行测试脚本

```bash
# 测试屏幕捕获
python scripts/test_capture.py

# 测试YOLO检测（摄像头）
python scripts/test_detection.py models/best.pt

# 测试YOLO检测（屏幕）
python scripts/test_detection.py models/best.pt -s

# 测试输入控制
python scripts/test_control.py
```

---

## 八、扩展开发指南

### 8.1 添加自定义状态

编辑 `src/decision/state_machine.py`:

```python
# 在 create_game_state_machine() 中添加新状态规则
def create_game_state_machine():
    sm = StateMachine()
    sm.add_rule(GameState.PLAYING, 
                lambda ctx: ctx.has_enemies(),
                GameState.COMBAT)
    return sm
```

### 8.2 添加状态回调

```python
engine = GameEngine(config=config)

def on_combat(context):
    target = context.get_nearest_enemy()
    if target:
        engine.controller.mouse_move(target.center[0], target.center[1])
        engine.controller.mouse_click()

engine.set_state_callback(GameState.COMBAT, on_combat)
engine.start()
```

### 8.3 添加新检测类别

在 `detector.py` 的 `CLASS_MAPPING` 中添加:

```python
CLASS_MAPPING = {
    'enemies': ['monster', 'hero', 'elite', 'boss', 'boss-m'],
    'doors': ['door'],
    'items': ['article', 'purple_card'],
    'ui_elements': ['brand', 'menu', 'shop'],
    'players': ['people'],
    'new_category': ['new_class_name']  # 添加新类别
}
```

---

## 九、项目目录结构

```
py-test/
├── main.py                    # 主入口文件
├── CLAUDE.md                   # 项目说明文档
├── Dnfv2_description.md       # DNF功能详细说明
├── requirements.txt            # 依赖列表
│
├── config/
│   └── settings.yaml          # 主配置文件
│
├── models/                    # YOLO模型文件
│   ├── best_sy.pt            # 深渊模式模型
│   ├── best_dxc.pt           # 地下城模型
│   └── *.pdiparams            # 模型参数
│
├── src/                      # 源代码
│   ├── __init__.py
│   │
│   ├── core/                 # 核心引擎
│   │   ├── engine.py         # 主引擎
│   │   ├── dungeon_runner.py # 副本运行器
│   │   └── scheduler.py     # 定时调度
│   │
│   ├── capture/              # 屏幕捕获
│   │   ├── mss_capture.py   # MSS捕获
│   │   └── window_manager.py # 窗口管理
│   │
│   ├── detection/            # 目标检测
│   │   └── detector.py      # YOLO检测器
│   │
│   ├── control/              # 输入控制
│   │   └── input_controller.py  # 键鼠模拟
│   │
│   ├── decision/             # 决策逻辑
│   │   ├── game_context.py  # 游戏上下文
│   │   ├── state_machine.py # 状态机
│   │   ├── skill_manager.py # 技能管理
│   │   ├── buff_manager.py  # Buff管理
│   │   ├── map_navigator.py # 地图导航
│   │   ├── card_flipper.py  # 翻牌处理
│   │   ├── stuck_handler.py # 卡住恢复
│   │   ├── character_switcher.py    # 角色切换
│   │   ├── multi_character_manager.py # 多角色管理
│   │   └── behavior_tree/   # 行为树(扩展)
│   │
│   ├── selection/            # 选择管理
│   │   └── selector.py     # 角色/地图选择
│   │
│   ├── utils/               # 工具函数
│   │   ├── config_loader.py # 配置加载
│   │   └── logger.py        # 日志系统
│   │
│   └── debug/               # 调试工具
│       └── visualizer.py    # 可视化调试
│
├── scripts/                  # 测试脚本
│   ├── test_capture.py      # 屏幕捕获测试
│   ├── test_detection.py    # 检测测试
│   └── test_control.py      # 输入控制测试
│
├── data/                     # 数据目录
│   ├── screenshots/          # 截图保存
│   └── *.jpg                 # 训练数据
│
└── logs/                     # 日志目录
    └── *.log                 # 日志文件
```

---

## 十、技术亮点

### 10.1 防检测设计

1. **贝塞尔曲线鼠标移动**: 模拟人类不规则轨迹
2. **随机延迟**: 每次按键间隔随机化
3. **人性化参数**: 延迟范围 [0.02, 0.08] 秒

### 10.2 智能决策

1. **多状态自动切换**: 根据检测结果自动进入对应状态
2. **紫卡优先策略**: 翻牌时自动识别并优先选择紫卡
3. **卡住自恢复**: 自动检测并脱离卡住状态

### 10.3 模块化设计

1. **插件式模块**: 各功能模块独立，可按需启用
2. **配置驱动**: 所有参数均可通过YAML配置
3. **扩展性强**: 预留行为树等高级决策接口

---

*文档版本: 1.0*
*生成日期: 2026-04-10*
