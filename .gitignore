__pycache__/
*.py[cod]
.venv/
logs/

# 运行时自动截图（量大，实测 1.2G+），不入库
data/screenshots/

# IDE
.idea/

# Claude Code 本地配置
.claude/

# TensorRT 引擎：体积大且与 GPU/驱动绑定，换卡需重建，不入库
models/*.engine
# ONNX 导出件可由 .pt 随时重新导出（scripts/export_cpu_onnx.py），不入库
models/*.onnx
# 校准参考截图（BMP 伪装成 .png，~20MB），不入库
0a42efd2-*.png
