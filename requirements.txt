# Game Autopilot - Python + YOLOv8 PC Game Automation

# Core dependencies
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0

# Screen capture
mss>=9.0.0
bettercam>=1.0.0
pillow>=10.0.0

# Input control
pynput>=1.7.6
pydirectinput>=1.0.4

# Windows API
pywin32>=306

# Configuration
pyyaml>=6.0

# Logging
colorama>=0.4.6

# Optional performance optimization
# onnxruntime-gpu>=1.16.0
# numba>=0.58.0

# OCR (RapidOCR - ONNX based, no Paddle dependency)
# 注意：旧的 rapidocr-onnxruntime 声明 requires_python<3.13，在 3.14 上装不了；
# rapidocr v3 是继任包（API 已适配，见 src/detection/ocr_detector.py）
rapidocr>=3.0.0
